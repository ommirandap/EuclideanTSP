import static org.junit.Assert.assertEquals;

import java.util.ArrayList;
import java.util.List;
import java.util.ListIterator;
import java.util.NoSuchElementException;

import org.junit.Test;

import cc4102.tarea3.geom.Point;

public class PointCircuitTests {

	/*@Test
	public void test() {
		List<Point> pc = new ArrayList<Point>(); 
		Point p1 = new Point(0,0);	
		Point p2 = new Point(1,1);
		pc.add(p1);
		pc.add(p2);
		Circuit
		ListIterator<Point> iterator = pc.circuitIterator();
		assertEquals(p1, iterator.next());
		assertEquals(p2, iterator.next());
		assertEquals(p1, iterator.next());
		assertEquals(p1, iterator.previous());
		assertEquals(p2, iterator.previous());
	}
	
	@Test(expected=NoSuchElementException.class)
	public void emptyListTest() {
		PointCircuit pc = new PointCircuit();
		ListIterator<Point> iterator = pc.circuitIterator();
		iterator.next();
	}*/

}
